<% if indexes.invalid.any? %>
  <div class="content">
    <h1>Invalid Indexes</h1>

    <p>These indexes exist, but can’t be used. You should recreate them.</p>

    <table class="table">
      <thead>
        <tr>
          <th>Name</th>
        </tr>
      </thead>
      <tbody>
        <% indexes.invalid.each do |query| %>
          <tr>
            <td><%= query["index"] %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>

